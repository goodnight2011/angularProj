import { GenericContDirective } from './generic-cont.directive';

describe('GenericContDirective', () => {
  it('should create an instance', () => {
    const directive = new GenericContDirective();
    expect(directive).toBeTruthy();
  });
});
